% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_minimal_white.R
\name{theme_minimal_white}
\alias{theme_minimal_white}
\title{Minimal Theme with White Background and Custom Axis/Grid Options}
\usage{
theme_minimal_white(
  base_size = 11,
  base_family = "",
  base_line_size = base_size/22,
  base_rect_size = base_size/22,
  base_col = "#323232",
  axis_col = base_col,
  grid = TRUE,
  axis = FALSE,
  ticks = FALSE,
  border = FALSE
)
}
\arguments{
\item{base_size}{Numeric. Base font size (default: 11).}

\item{base_family}{Character. Base font family (default: "").}

\item{base_line_size}{Numeric. Base line size relative to `base_size` (default: `base_size / 22`).}

\item{base_rect_size}{Numeric. Base rectangle size relative to `base_size` (default: `base_size / 22`).}

\item{base_col}{Character. Default color for axes, ticks, and borders (default: "#323232").}

\item{axis_col}{Character. Color of axis lines and ticks. Defaults to `base_col`.}

\item{grid}{Logical or Character. Controls the visibility of grid lines:
\itemize{
  \item \code{TRUE} (default): Shows major and minor grid lines.
  \item \code{FALSE}: Hides all grid lines.
  \item Character string: Allows selective control using "X", "Y", "x", "y":
    \itemize{
      \item \code{"X"}: Shows major grid lines on the x-axis.
      \item \code{"Y"}: Shows major grid lines on the y-axis.
      \item \code{"x"}: Shows minor grid lines on the x-axis.
      \item \code{"y"}: Shows minor grid lines on the y-axis.
    }
}}

\item{axis}{Logical or Character. Controls axis lines:
\itemize{
  \item \code{TRUE}: Shows both x and y axis lines.
  \item \code{FALSE} (default): Hides all axis lines.
  \item Character string: Selectively controls axis lines:
    \itemize{
      \item \code{"x"}: Shows only the x-axis line.
      \item \code{"y"}: Shows only the y-axis line.
      \item \code{"xy"}: Shows both axis lines.
    }
}}

\item{ticks}{Logical. Whether to show axis ticks (default: `FALSE`).}

\item{border}{Logical. Whether to add a panel border around the plot (default: `FALSE`).
If \code{TRUE}, it overrides the `axis` argument and hides axis lines.}
}
\value{
A \code{ggplot2} theme object with a white plot background and customizable
  grid, axis, tick, and border settings.
}
\description{
This function extends \code{ggplot2::theme_minimal()} by ensuring a white background
(preventing gray backgrounds when exporting to PNG) and providing options for customizing
axis lines, grid visibility, axis ticks, and borders.
}
\examples{
library(ggplot2)
base_plot <- ggplot(mtcars,
                    aes(x = mpg,
                        y = wt)) +
  geom_point()

base_plot

base_plot +
  theme_minimal_white()

base_plot +
  theme_minimal_white(grid = "XY")

base_plot +
  theme_minimal_white(axis = "X")

base_plot +
  theme_minimal_white(border = TRUE)

base_plot +
  theme_minimal_white(grid = FALSE,
                      border = TRUE,
                      ticks = TRUE)

}
